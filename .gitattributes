# Normalize line endings so the repo stays consistent between Windows,
# macOS and the Linux builders that Vercel and Netlify run on. Without this,
# switching machines can show every line of a file as changed.
* text=auto eol=lf

# Binary files — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
